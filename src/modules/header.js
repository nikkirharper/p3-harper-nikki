class Header {
    constructor(pageTitle, randomizerButton) {
        this.pageTitle = pageTitle; 
        this.randomizerButton = randomizerButton; 
    }
}

export const header = new Header("Hungry?", "View Today's Lunch Specials >");